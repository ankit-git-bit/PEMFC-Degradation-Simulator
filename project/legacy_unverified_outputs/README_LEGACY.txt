These files were present in the project before this engagement's audit
and bug-fixing pass (see ../VALIDATION_REPORT.md). They were generated by
generate_presentation_data.m, which internally uses its own simplified,
self-contained ODE implementation (not the corrected master pipeline in
ode_system.m / lifetime_simulation.m). Because that file's local ODE has
not been independently re-verified (and in fact timed out during testing
in this engagement -- see VALIDATION_REPORT.md, Section 5), these figures
should NOT be treated as validated output of the corrected model. They are
kept only for reference/comparison. Regenerate figures from
demonstration_run_120h.mat or a fresh lifetime_simulation.m run instead.
